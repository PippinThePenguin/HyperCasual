using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class CellScript : MonoBehaviour
{
  /*  
  public BlockScript Block;
    [SerializeField] private string msg;
    [SerializeField] private ElGeneriko.MathCubes effect;    
    [SerializeField] private Material whiteMat, blackMat;
    public ElGeneriko.Color PlayerColor;
    public int Value;

    [SerializeField] private SingleBell bell;



    private void OnTriggerEnter(Collider collider)
    {
    //Debug.Log(collider.tag);
        if (collider.tag == "Player")
        {
            //Debug.Log("pig");
            //bell.Bonk();
            Block.Activation(msg, effect);
        }

    }
    private void OnEnable()
    {
        //effect = new ElGeneriko.Cubes(PlayerColor, Value);
    }

    public void OnActivation(ElGeneriko.MathCubes newCube = null)
    {
        if (newCube != null)
        {
            effect = newCube;
            bell.ChangeColor(newCube.color);
            bell.Reset();
            
        }
    }
  */
}
